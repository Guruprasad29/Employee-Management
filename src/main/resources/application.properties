server.port=8080
spring.application.name=employee-management
# Developer C updates database configuration in the Git conflict exercise.
# The demo application intentionally does not require a live database.
app.database.url=${DB_URL:jdbc:h2:mem:employee}
app.database.username=${DB_USERNAME:sa}
app.database.password=${DB_PASSWORD:}
