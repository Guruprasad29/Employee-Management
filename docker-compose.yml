services:
  employee-management:
    build: .
    container_name: employee-management
    ports:
      - "8080:8080"
    environment:
      DB_URL: jdbc:h2:mem:employee
      DB_USERNAME: sa
      DB_PASSWORD: ""
